name: 'Post Hotfix'
description: 'Post Hotfix Action'
author: 'Rojan Gh.'
inputs:
  token:
    required: true
    description: GitHub Token
  branch_name:
    required: true
    description: Hotfix branch name
  release_version:
    required: true
    description: The hotfix release version
  stage:
    required: true
    description: The stage of the hotfix
runs:
  using: 'node20'
  main: 'dist/index.js'
